def is_able_to_travel(iet: float, interval: int) -> bool:
  """Validates if is able to travel after a given interval.

  Args:
      iet (float): initial execution time
      interval (int): amount of time to wait before travel
  """
  # code that is almost working
  # travel_interval = time.time() + 60
  # travel_time = initial_execution_time
  # print('interval:', travel_interval)
  # print('time:', travel_time)
  # print(time.time())
  # if time.time() - travel_time == travel_interval - time.time() and pag.pixel(1105, 30)[0] == 255:
  #   pag.click(1105, 30)
  #   travel_time = time.time()
  #   print('in if:', travel_time)
